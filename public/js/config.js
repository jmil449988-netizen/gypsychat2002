// This file is retired -- config now lives in appconfig.js (a fresh path, since this exact URL
// got stuck serving a stale cached copy on Cloudflare's edge across several deploys). Nothing
// references config.js anymore; it's kept only as an empty placeholder rather than deleted.
